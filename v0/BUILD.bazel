load("@rules_abcue//cue:defs.bzl", "cue_def", "cue_export", "cue_instance")

cue_instance(
    name = "cue_instance",
    package_name = "kue",
    srcs = glob(["*.cue"]),
    ancestor = "//cue.mod",
)

cue_def(
    name = "cue_def",
    instance = ":cue_instance",
    package = "kue",
)
